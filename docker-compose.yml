version: '3.8'

services:
  app:
    build: .
    container_name: ymarket-app
    environment:
      - TZ=Asia/Seoul
    volumes:
      - /srv/ymarket:/data

# No volumes section needed if using direct host bind in services
